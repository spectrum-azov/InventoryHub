/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}",
  ],
  theme: {
    extend: {
      colors: {
        // Custom colors if needed, but Tailwind defaults are great
      }
    },
  },
  plugins: [],
}
